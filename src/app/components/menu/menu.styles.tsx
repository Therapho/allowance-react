import { mergeStyleSets } from "@fluentui/merge-styles";

export const menuStyles = mergeStyleSets({
  root: {
    width: 208,
    height: "100%",
    boxSizing: "border-box",
    overflowY: "auto",
  },
});

import { mergeStyles } from "@fluentui/merge-styles";

export const AddTransactionStyles = {
    fieldSet: mergeStyles({
        width:"100%"
    }),
    label:mergeStyles({
        width:"50%"
    }),
    control:mergeStyles({
        width:"50%"
    })
}

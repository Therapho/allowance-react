import { mergeStyles, mergeStyleSets } from "@fluentui/react"

export const cardStyles = mergeStyleSets ({
    text:{
    color: "red"
    }})
  export const cardStyle = mergeStyles({
    color:"blue"
  })